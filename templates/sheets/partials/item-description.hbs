<div class="item-content">
  <div class="field-name">{{localize "CLEENMAIN.label.description"}}</div>
  {{#if unlocked}}
    {{formInput systemFields.description enriched=descriptionhtml value=system.description}}
  {{else}}
    {{{descriptionhtml}}}
  {{/if}}
</div>